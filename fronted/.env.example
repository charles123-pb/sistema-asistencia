# Frontend environment example (Angular)
# For dev you can use proxy; for direct API calls set API_URL

# API base (without trailing slash)
API_URL=http://localhost:3001/api

# Timeouts (ms)
API_TIMEOUT=30000

# Production build should set production-specific API_URL
# API_URL=https://api.tudominio.com/api
